import { getDoc, doc } from "firebase/firestore";
import { db } from "../../services/firebase";
import getCodeFromWeekdays from "../../functions/getCodeFromWeekdays";

export default async function getGlobalKeyCode({
  keyParam,
  setCodeData,
  setLoaded,
}) {}
